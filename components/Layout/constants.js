import {
  ABOUT_IDENTIFIER,
  CONTACT_IDENTIFIER,
  PROJECTS_IDENTIFIER,
} from 'templates/HomeTemplate/constants'

export const menuItems = [
  { text: 'About', href: '#' + ABOUT_IDENTIFIER },
  { text: 'Projects', href: '#' + PROJECTS_IDENTIFIER },
  { text: 'Contact', href: '#' + CONTACT_IDENTIFIER },
]
